# Claude Pet

## 중복 작업 방지

작업을 시작하기 전에 반드시 기존 작업 현황을 확인한다:

1. `gh pr list --state all` 로 기존 PR 확인
2. `git branch -r` 로 원격 브랜치 확인
3. 이미 동일한 작업에 대한 PR이나 브랜치가 존재하면 건너뛰고 사용자에게 알린다
4. closed PR이 있으면 재작업이 필요한지 사용자에게 확인한다
